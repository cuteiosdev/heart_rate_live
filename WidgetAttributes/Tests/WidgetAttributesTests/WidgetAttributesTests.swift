import XCTest
@testable import WidgetAttributes

final class WidgetAttributesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WidgetAttributes().text, "Hello, World!")
    }
}
